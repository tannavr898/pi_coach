# Copy to .env and fill in. NEVER commit the real .env.
# Hard rule (roadmap §5/§9): every provider key lives ONLY on the backend.
# The frontend talks only to our /api/* endpoints; it never sees these.

# Anthropic API key — used for scenario generation + content scoring (Phase 2).
# Not called yet in this scaffold step.
ANTHROPIC_API_KEY=

# Transcription API key (Phase 3, voice pipeline). Leave blank for now.
# TRANSCRIPTION_API_KEY=
