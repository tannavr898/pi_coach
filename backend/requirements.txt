# Runtime deps for the PI Coach backend (mirrors pyproject.toml [project].dependencies).
# Used by the Docker build; for local dev `uv sync` / editable install also works.
fastapi>=0.115
uvicorn[standard]>=0.32
anthropic>=0.40
python-dotenv>=1.0
truststore>=0.9
httpx>=0.27
python-multipart>=0.0.9
